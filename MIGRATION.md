# Инструкция по переносу проекта на другую машину

## Что нужно перенести

### 1. Весь код проекта
Перенесите всю папку проекта, **исключая**:
- `node_modules/` (папки с зависимостями)
- `dist/` (скомпилированные файлы)
- `.git/` (если не используете git)

### 2. База данных
Если база данных уже создана и содержит данные:
- `data/cinema.db` - основная база данных
- `data/cinema.db-shm` - файл shared memory (если есть)
- `data/cinema.db-wal` - файл write-ahead log (если есть)

**Важно**: Переносите все три файла вместе, если они существуют.

### 3. Видео файлы (если есть)
- `data/videos/` - папка с видео файлами премьер

### 4. Конфигурация
- `backend/src/config.ts` - содержит API ключи и настройки администратора

## Пошаговая инструкция

### На старой машине (откуда переносим)

#### Вариант 1: Через архив (рекомендуется)

1. **Создайте архив проекта** (исключая ненужные папки):
   ```powershell
   # В PowerShell
   Compress-Archive -Path .\* -DestinationPath cinema-board-backup.zip -Exclude node_modules,dist,.git
   ```

   Или используйте WinRAR/7-Zip и исключите:
   - `node_modules/`
   - `dist/`
   - `.git/` (опционально)

2. **Скопируйте архив** на новую машину (через USB, сеть, облако и т.д.)

#### Вариант 2: Через Git (если используете)

1. **Закоммитьте все изменения**:
   ```bash
   git add .
   git commit -m "Backup before migration"
   ```

2. **Запушьте в репозиторий**:
   ```bash
   git push
   ```

3. На новой машине клонируйте репозиторий

#### Вариант 3: Ручное копирование

Скопируйте всю папку проекта, затем удалите на новой машине:
- `node_modules/` (в корне, в `backend/`, в `frontend/`)
- `dist/` (если есть)

### На новой машине (куда переносим)

1. **Убедитесь, что установлены**:
   - Node.js (версия 18 или выше)
   - npm (обычно идет с Node.js)

2. **Распакуйте проект** (если использовали архив) или скопируйте папку

3. **Установите зависимости**:
   ```bash
   npm run install:all
   ```
   
   Или вручную:
   ```bash
   npm install
   cd backend && npm install && cd ..
   cd frontend && npm install && cd ..
   ```

4. **Проверьте структуру папок**:
   Убедитесь, что существует папка `data/` в корне проекта:
   ```
   cinemadelux_5panel/
   ├── data/
   │   ├── cinema.db (если переносили)
   │   └── videos/ (если есть видео)
   ├── backend/
   ├── frontend/
   └── ...
   ```

5. **Проверьте конфигурацию**:
   Откройте `backend/src/config.ts` и убедитесь, что:
   - API ключ Кинопоиска актуален
   - Пароль администратора установлен правильно

6. **Запустите проект**:
   ```bash
   # Режим разработки
   npm run dev
   
   # Или production сборка
   npm run build
   npm run start
   ```

## Важные замечания

### Если база данных не переносилась
Если вы не переносили `data/cinema.db`, база данных создастся автоматически при первом запуске с:
- 5 залами (Зал 1, Зал 2, ..., Зал 5)
- Пользователем-администратором (логин: `admin`, пароль из `config.ts`)

### Если база данных переносилась
1. Убедитесь, что перенесли все файлы БД (`cinema.db`, `cinema.db-shm`, `cinema.db-wal`)
2. Проверьте права доступа к файлам БД
3. При первом запуске база данных будет использована как есть

### Проверка после переноса

1. **Запустите проект**:
   ```bash
   npm run dev
   ```

2. **Проверьте работу**:
   - Откройте табло: http://localhost:5173/board
   - Войдите в админку: http://localhost:5173/admin
   - Проверьте, что данные отображаются корректно

3. **Если что-то не работает**:
   - Проверьте консоль на ошибки
   - Убедитесь, что все зависимости установлены
   - Проверьте, что база данных на месте и доступна

## Быстрая проверка

После переноса выполните:

```bash
# Проверка версии Node.js
node --version

# Установка зависимостей
npm run install:all

# Запуск
npm run dev
```

Если всё работает - перенос выполнен успешно! 🎉
